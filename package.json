{
  "name": "spellbook",
  "version": "0.1.0",
  "private": true,
  "description": "Spell preparation planner between long rests.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/serve-app.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}
